# AGENTS.md — Grok Bot / Grok Build 인수인계

이 저장소를 이어 작업하라. 새 앱을 만들지 말고 여기 구조를 확장하라.

## 제품

한국거래소(KRX) 상장 ETF 중심의 포트폴리오 과거 시뮬레이터. 가격 시뮬은 **수정주가 기준(분배금 세전 재투자 효과 포함) · 세금 미반영 · 과거 시뮬**.
비교 목적으로 일부 **미국 상장 ETF**를 「배당 현금흐름」 모드의 「해외 직투 비교」 그룹(`data/div_meta.json` `market:"US"`)으로만 제공한다. 미국 ETF는 **기본 프리셋·기본 전략·MOM/DMOM/XSMOM/MOM12_1/invVol 후보군(`etf_meta.json`)에 넣지 않는다**(가격 시뮬 결과 바이트 동일 유지). 레버리지/인버스 미국 ETF는 포함하지 않는다.

- 실행: `python3 serve.py` → http://127.0.0.1:8765
- 시세 갱신: `python3 agents/fast_ingest.py` (시총 상위 ~80, 메타+선택 로드용 종가)
- 검수: `python3 agents/reviewer.py` 반드시 PASS 후에만 UI 변경 머지

## 역할 분리 (숫자를 모델이 계산하지 말 것)

| 역할 | 파일 | 규칙 |
|---|---|---|
| Fast | `agents/fast_ingest.py` | 목록·종가만. 해석 금지 |
| Build | `agents/build_backtest.py`, `app.js`, `index.html`, `styles.css` | 엔진과 화면. JS와 Python 로직을 같게 유지 |
| Review | `agents/reviewer.py` + 화면 리뷰 패널 | 불변식 검사. 투자 추천 문장 금지 |

## 불변식

- 단일 ETF 바이앤홀드 총수익률 = 기간 첫 종가 대비 마지막 종가
- 리밸런싱은 **당일 평가 후** 비중 재조정. 리밸런싱 날 수익률을 지우면 안 됨
- 월 적립(DCA): 매월 첫 거래일에 현금 유입 **후** 목표 비중으로 **추가 매수만**(기존 보유 매도 없음, 당일 평가 이후). 그날이 캘린더 리밸런싱일이면 현금 포함 전체 재조정
- **전체 재조정은 실제 리밸런싱일에만**: Q/Y/M 캘린더(또는 밴드 이탈), MOM류 월간 교체, day0. 월간 오버레이(국면헤지·슬리브추세·변동성타깃·GOLDON)는 비캘린더 월에 **슬리브만 갱신** — 가상 코어 장부(마지막 전체 재조정 이후 가격대로 드리프트한 base 비중)에 오버레이를 다시 적용하므로 코어 보유 간 상대 드리프트 유지. **MA 오버레이 신호는 리밸 주기와 별도로 매월 판단**(`ma_signal_freq`/`maSignalFreq`=`monthly` 기본): 매월 첫 거래일에 **전월 말 종가**(asof=`YYYY-MM-01` 이전 종가만) vs SMA. MA는 월간 오버레이 체인의 첫 단계(MA→국면헤지→슬리브추세→변동성타깃→GOLDON)로 드리프트한 가상 코어(MA 이전 base)에 적용 → 비캘린더 월 전환 시 위험자산↔현금(`153130`) 부분만 이동(`sleeve_update_count`, 비용은 실제 전환분 TO만). MA 단독은 신호 전환 월에만 슬리브 갱신, 다른 월간 오버레이와 함께면 매월 체인 재적용. `ma_switch_count`=월초 신호 전환 횟수(캘린더 무관). `rebal`=과거(8d9fc50) 동작(리밸런싱일에만). invVol은 리밸런싱일에만 갱신. `N`은 오버레이 슬리브 이동·적립 매수 외 바이앤홀드
- 비용: 비캘린더 월 슬리브 갱신·적립 매수도 동일 식 `TO = 0.5×Σ|w_new−w_old|`(현금 유입분 미투자 상태 기준 → 순수 적립 매수 = 0.5×적립액×비용률, 이전과 동일). `rebal_count`=전체 재조정 횟수(day0 제외), `sleeve_update_count`·`dca_buy_count` 별도
- MDD ≤ 0
- 가격 > 0
- 공통 거래일 20일 미만이면 에러
- 투자 자문처럼 쓰지 말 것. 시뮬레이터 고지 유지
- 가격 시뮬 시세(FDR/NAVER, Yahoo `auto_adjust=True` 백필)는 **분배락을 되돌린 수정주가**다 → 고지는 「수정주가 기준(분배금 세전 재투자 효과 포함) · 세금 미반영 · 과거 시뮬」. 「가격수익률·분배금 미반영」이라고 쓰지 말 것
- 수정주가 위에 추정 분배율을 다시 얹는 **합성 TR 금지**(이중 계산). TR 토글은 영구 비활성(`hasRealTrData()`=false, `buildTotalReturnPrices` no-op)
- 절세 모형(일반계좌): 추정 분배금 15.4% + 매매차익(= 기말 − 원금 − 추정 분배금) × (국내주식형 0% / 그 외 15.4%) — 분배금 이중과세 금지
- **배당 모드**는 **원가격(분할만 조정) 시계열 + 실제 분배 이벤트**로만 계산(`backtest_dividend` / `backtestDividend`, 기존 `backtest`와 분리). `priceBasis`가 `raw`/`raw_split_adjusted`가 아니면 FAIL. 수정주가(`data/prices`, `etf_prices.json`)를 배당 모드에 쓰지 말 것
- 배당 모드 하루 순서: MTM(미국=USD×전일 환율) → 분배(배당락 prev<ex≤d, 전일 종가 보유 수량, 환율은 ex 이전 마지막 값, 세율 미국 15%/국내 15.4%) → 인출(장부 적립) 또는 같은 종목 재투자(비용 0.5×금액×비용률) → 월 적립 → 밴드 → 전체 재조정. 고정 비중(Q/Y/M/N)+밴드+적립만
- 분배금이 없는 달은 0, **데이터 범위 밖은 「데이터 없음」, 정기 일정상 빠진 달은 「미확인」**(0으로 채우지 말 것). 월 집계는 **배당락일 기준**
- 문구: 「월 ○원 받는다/월급처럼/추천/순위/최고」 금지 → 「과거 기준 ○원이었다」. TTM 월평균은 「월평균 = TTM÷12」 라벨

## 데이터

- 소스: FinanceDataReader (KRX/NAVER); FDR 이전은 `agents/yf_backfill.py`로 Yahoo `{code}.KS` 병합 가능
- 메타: `data/etf_meta.json` (UI 목록) — GitHub Pages용으로 커밋 가능
- 종가: `data/etf_prices.json` (번들, Pages/demo) + `data/prices/{code}.json` (선택 로드, gitignore)
- Pages 데모를 위해 `etf_prices.json` / `etf_meta.json` 은 추적. `data/prices/` 만 무시
- 주의: FDR(NAVER) 국내 ETF 종가는 **분배 반영 수정주가**다(예: KODEX 200 2020-04-28 원가격 25,650 vs FDR 22,774)
- 배당 모드 데이터(`python3 agents/div_ingest.py`, 추적):
  - 미국 ETF: Yahoo `auto_adjust=False` Close(분할만 조정) → `data/us/prices/{T}.json` (+연말 `adjAnchors` = Yahoo Adj, 리뷰 검증용)
  - 분배: 발행사(SCHD Schwab `data/sources/`) > Nasdaq API > stockanalysis > Yahoo 우선순위 병합, 대조값 `chk` → `data/dividends/{T}.json`. Yahoo 결함(QQQ 2020-09 누락, 2010-06-25 중복, BND 금액 오류)은 교정. 빠진 달은 `gaps`(미확인)
  - 국내 분배금: KRX KIND `ETF이익금분배신고` 공시(접수번호 `src`) → `data/dividends/{code}.json`; 국내 원가격 `data/prices_raw/{code}.json`(Yahoo `.KS` Close, auto_adjust=False). 배당락일 = 기준일 직전 KRX 거래일
  - 환율: FRED `DEXKOUS` 기준 + 최근분 Yahoo `KRW=X`(주말 행 제외) → `data/fx/USDKRW.json`
  - 목록·범위·미확인 구간: `data/div_meta.json`
- pykrx는 KRX 로그인 필요할 수 있어 기본 경로로 쓰지 말 것
- 유니버스는 시총 상위 유동성 KRX ETF. 레버리지/인버스는 태그 달고 기본 프리셋에 넣지 말 것

## 리밸런싱 모드

| 코드 | 의미 |
|---|---|
| `Q` | 분기 리밸런싱 (목표 비중) |
| `Y` | 연 1회 |
| `M` | 매월 고정 비중 |
| `MOM` | 월간 모멘텀 (전월 말 기준 1m/3m 수익률 상위 N 동일비중, 교체 회전율×거래비용, 룩어헤드 금지) |
| `MOM12_1` | 12-1 스킵 모멘텀 (전월 말 신호 · 최근 1개월 제외 12개월 수익률 상위 N 동일비중) |
| `XSMOM` | XS 잔차 모멘텀 (룩백 수익률 − 유니버스 EW 평균 · 상위 N · 유니버스 <5 시 에러) |
| `DMOM` | 듀얼 모멘텀 (상대 모멘텀 + 안전자산 대비 절대 필터, 기본 현금대리 `153130`) |
| `N` | 없음 |

**리밸런싱 밴드** (`bandOn` + `bandPct` 1–10%, 기본 5%):

- 고정 비중 모드(`Q`/`Y`/`M`/`N`)에서 ON이면 **캘린더 일정을 쓰지 않고** 매일 목표 비중 대비 드리프트를 검사한다.
- 어느 한 종목이 `|현재비중 − 목표비중| > bandPct`이면 당일 평가 후 목표 비중으로 리밸런싱한다(기존 당일 MTM→리밸 규칙과 동일).
- `MOM`/`MOM12_1`/`XSMOM`/`DMOM`에는 적용하지 않는다(월간 교체 로직 유지). UI에 고지.
- 밴드 ON에서도 DCA는 추가 매수만, 국면헤지·금 슬리브·변동성 타깃·슬리브 추세는 월초 슬리브만 갱신(전체 재조정 아님). 밴드 기준 = 현재 오버레이 상태의 목표 비중.
- **거래비용** (`tradeCost` / `mom_cost`, 기본 0.1%=10bps, UI 0–0.5%): 캘린더·밴드·MOM/DMOM·금 전환 등 **모든 리밸런싱**에서
  `value -= value × TO × rate`, `TO = 0.5 × Σ|Δw|`(편도 회전율). 초기 day0 편입은 미적용. 결과·CSV에 가정 비용률·누적 드래그 표시.

**시작일 민감도 히트맵** (배치6, `startDateSensitivity` / `start_date_sensitivity`):

- 현재 포트 설정(보유·리밸·오버레이·비용 등)을 고정하고 **종료일은 데이터 끝**, 시작월만 바꿔 종료창 CAGR·MDD를 격자(연×월)로 표시.
- UI는 결과 옆 「민감도 보기」로만 실행(자동 실행 금지). 상한 `SENSITIVITY_MAX_STARTS`(120); 초과 시 최근 구간 월간. 청크 비동기.
- 과거 시뮬 · 투자 자문 아님.


추가 축(리밸런싱과 독립/병행):

- `weighting=invVol`: 리밸런싱 직전 60거래일 실현변동성 역수 비중 (고정 입력비중 `fixed`와 선택). MOM/DMOM 편입 집합에도 적용 가능
- `maOverlay`: 벤치마크 `069500` vs SMA(기본 200) 국면 오버레이. 위험오프 시 안전자산(`153130` 기본) 비중
- 기본 안전자산은 레버리지/인버스가 아닌 `153130`(장기) / `214980`·`423160` 대체
- **실험** `regimeHedge`(국면 헤지): 069500∧133690 둘 다 MA↓일 때만 −1x `114800` 또는 현금/단기채 ≤15%, 월1회. 2X 금지. 기본 프리셋·기본 전략에 넣지 말 것. 상세 `docs/EXPERIMENT_REGIME_HEDGE.md`
- `goldOn`(금 온/오프 슬리브): 신호 `411060`/`132030` vs 현금대리 `153130` 절대모멘텀(룩백 1|3, 전월 말). ON이면 슬리브(기본 15%, UI 10–20)를 금에, OFF면 동일 비중을 단기채에. 나머지 비중은 사용자 포트 비율 유지. 월1회·상태 전환 시에만 슬리브 명목 0.1% 비용. 오버레이 순서: base→invVol→ma→regime→sleeveTrend→volTarget→**gold last**. `0072R0` 사용 금지
- `volTarget`(변동성 타깃): 위험 슬리브 트레일링 W일 실현변동성(연율) ≈ 목표 σ(기본 10%). 스케일 ≤100%, 잔여 → 현금대리(기본 `153130`). asof 이전 종가만. 레버리지/인버스 ETF는 이 경로에서 제외. 월1회 갱신
- `sleeveTrend`(슬리브 추세): 메타 카테고리(국내주식/해외주식/채권/원자재/테마 등)별 절대모멘텀 또는 MA ON/OFF. OFF 비중 → 현금. 결측=OFF. 현금성 유지. 전월 말·asof 이전만

## 다음 작업 우선순위

1. ~~ETF 검색 + 카테고리 필터. 목록을 늘리되 시세는 선택 종목만 로드~~ (완료)
2. ~~월 적립(DCA) 시뮬레이션~~ (완료)
3. ~~분배금 데이터가 있으면 총수익(TR) 토글. 없으면 가격수익 고지를 더 명확히~~ (배치5: 가격수익 고지; 실제 TR 시계열 있을 때만 토글) → **div1 정정**: 시세가 수정주가라 고지를 「수정주가 기준(분배금 세전 재투자 효과 포함) · 세금 미반영 · 과거 시뮬」로 바꾸고 TR 토글 폐지, 실제 분배금은 배당 현금흐름 모드
4. ~~모바일 레이아웃 다듬기~~ (완료)
5. 증권사 주문 연동은 하지 말 것 (규제)
6. 선택 종목 시세만 다시 받는 부분 ingest / UI 캐시 개선
7. ~~시작일 민감도 히트맵~~ (배치6 완료 · 본 개선 시리즈 종료)

## 언어

UI와 커밋 메시지는 한국어. 코드 식별자는 영어.
